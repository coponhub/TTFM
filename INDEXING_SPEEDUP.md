# Indexing Speedup Plan: In-memory Metadata Filtering

数百万件規模のファイルシステムにおいても、正確かつ高速に変更を検知するための設計指針です。

## 1. 背景と課題
現在の `ttfm` は、全ファイルを走査（スキャン）し、そのメタデータを一度すべて DuckDB のテンポラリテーブルに投入してから差分を比較しています。
- **課題**: ファイル数が増える（数百万件〜）と、変更がないファイルについても「構造体作成 + チャンネル転送 + DB書き込み」が発生し、そこがボトルネックとなります。
- **要件**: ファイルの中身のみが編集された場合（`mtime` や `size` の変化）も確実に見逃さない必要があります。

## 2. 提案手法：インメモリ・メタデータ・フィルタリング

### A. キャッシュ構築 (Pre-scan Phase)
スキャンを開始する前に、既存のインデックスから「変更検知に必要な最小限の情報」をメモリにロードします。
- **対象データ**: `path`, `mtime`, `size`
- **保持形式**: `HashSet<u64>`
  - `(path, mtime, size)` を結合してハッシュ化（例: `FxHasher` 等を使用）。
- **メモリ効率**: 
  - 1件あたり 8バイト（`u64`） + HashSetのオーバーヘッド。
  - 100万件で約 10〜16MB 程度。1000万件でも 200MB 以下。
  - 非常に省メモリで、数百万〜数千万件のファイルをさばくことが可能。

### B. 早期フィルタリング (During-scan Phase)
`ignore` クレートによる並列走査中、各スキャンスレッドで以下の処理を行います。
1. `stat`（メタデータ取得）を実行。
2. `(path, mtime, size)` のハッシュを計算。
3. `HashSet` と照合。
   - **一致（変更なし）**: 処理をその場で終了。DB への送信（`mpsc::send`）を行わない。
   - **不一致（変更あり/新規）**: `ScanEntry` を作成し、チャンネル経由で DB に送る。

### C. 差分統合 (Post-scan Phase)
スキャンテーブル（`Tbl::Scan`）には「変更があったもの」だけが蓄積されます。
- `deleted` や `moved` の検知ロジックは、この「差分のみのスキャンテーブル」と「既存のマスターデータ」を比較するように調整します。

## 3. 書き込みの効率化：バッチ化とバルクロード

早期フィルタを通過した「新規・変更あり」のデータや、初回インデックス時の膨大なデータを高速に DB へ投入するための施策です。

### A. MPSC 転送のバッチ化 (Batching)
現状の 1 件ずつの送信を、数千件単位の `Vec<ScanEntry>` まとめて送信する方式に変更します。
- **効果**: チャンネルのロック競合が劇的に減少し、スキャンスレッドがメインスレッド（書き込み待ち）でブロックされる時間がなくなります。

### B. バルクロードの活用 (Parallel Bulk Loading)
DuckDB の `Appender` ではなく、SQL による一括ロード（`INSERT INTO ... SELECT FROM`）を活用します。

#### なぜ Appender より速いのか？
1. **ベクタライズ処理**: DuckDB は内部でデータを「ベクトル（塊）」として処理します。Appender のように 1 行ずつ関数を呼ぶオーバーヘッドがなく、CPU キャッシュを効率よく利用できます。
2. **列指向フォーマットの親和性**: メモリ上で Arrow 等の列指向形式で保持したデータを、DuckDB の内部ストレージへ最小限の変換で直接流し込めます。
3. **自動並列化**: SQL によるロードは DuckDB が内部でマルチスレッド処理を自動で行うため、書き込み側のボトルネックが解消されます。

## 4. 期待される効果
1. **DB I/O の劇的な削減**: 
   通常のユースケースではファイルの 99% 以上が「変更なし」であるため、DB への書き込み負荷が 1/100 以下になります。
2. **スキャン完了までの高速化**:
   重い `ScanEntry`（文字列を含む構造体）の生成やアロケーションがスキップされるため、`ttfm index --dry-run` に近い速度でインデックスが完了します。
3. **初回インデックスの底上げ**:
   フィルタが効かない初回スキャン時も、バルクロードとバッチ化により、ハードウェア（I/O）の限界に近い速度でインデックスを構築できます。
4. **正確性の担保**:
   ディレクトリの `mtime` ではなく、各ファイルの `mtime/size` を全件チェックするため、ファイルの内容編集を確実に見逃しません。
